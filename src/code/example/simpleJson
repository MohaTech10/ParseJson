{
    "key1": true,
    "key2": 2,
    "key3": {
        "home": "Mecca",
        "country": "KSA"
    },
    "Varrays": [
        "a", "b", "t", 1, [1, 3], {"object": "value"}
    ]
}

class Root {
    boolean key1;
    int key2;
    CustomClass key3;
    // if array is homogenous then craete Say -> list<KnownType>, list<String> list<Number>
    // else means it's heterogeneous array we call class creator
     customArrayType array

}
class CustomClass {
    String home;
    String ksa;
}
class CustomArrayType {
    // we may count consistent? so that if we have three homogenous
    string value; // maybe string[] values for a, b, c
    int integer;
    array<integer>
    CustomNestedOBject cno;
}
// array need a little bit of thinking and working